---
interface Props { variant?: 'fi'|'sv'|'dual'; label?: string }
const { variant = 'dual', label } = Astro.props;
---
<span class={`flag-ribbon flag-ribbon-${variant}`} aria-hidden={label ? undefined : 'true'} aria-label={label}><span></span><span></span></span>
